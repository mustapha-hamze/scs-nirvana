@model bool
@{
    Layout = null;
}

@if (Model)
{
    <button type="button" class="btn btn-primary" onclick="createNewContent()">
        <i class="uil uil-plus"></i>
        Create Content
    </button>
}
