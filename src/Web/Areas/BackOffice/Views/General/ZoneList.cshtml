@{
    Layout = null;
}
@model List<Infrastructure.Dto.GeneralDtos.ZoneDto>

<table id="datatable-buttons" class="table table-striped dt-responsive nowrap w-100">
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Parent</th>
            <th>Created</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @* @foreach (var item in Model)
                {
                <tr>
                <td>@item.Title</td>
                <td>
                @if (item.ParentId != 0)
                {
                var parent = Model.Single(z => z.Id == item.ParentId);
                <text>@parent.Title</text>
                }
                else
                {
                <text>-----</text>
                }
                </td>
                <td>@item.CreatedDT.ToString("yyyy MMM dd hh:ss")</td>
                <td>
                <a href="javascript: void(0);" class="action-icon">
                <i class="mdi mdi-delete"></i>
                </a>
                </td>
                </tr>
                } *@
    </tbody>
</table>