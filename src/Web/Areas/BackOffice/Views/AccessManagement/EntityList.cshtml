@{
    Layout = null;
}
@model List<Application.Contracts.AccessManagement.SectorEntityDto>

<table id="datatable-buttons" class="table table-striped dt-responsive nowrap w-100">
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Access Key</th>
            <th>Manage</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Title</td>
                <td>@item.AccessKey</td>
                <td>
                    <a href="javascript: void(0);" class="action-icon" title="Edit" aria-label="Edit @item.Title">
                        <i class="mdi mdi-content-save-edit"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>